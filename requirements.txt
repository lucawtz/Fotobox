# Versionen gepinnt, damit ein ./install.sh kurz vor dem Event nicht plötzlich
# einen neuen Major/Minor zieht und die Box am Veranstaltungstag anders läuft
# als beim Test.
#
# `~=` erlaubt bewusst nur Patch-Updates (z.B. flask 3.1.3 -> 3.1.4), aber
# keinen Sprung auf 3.2 oder 4.0. Exakte Pins stehen hier NICHT, weil die
# Entwicklungsmaschine (macOS/x86) und das Pi (Linux/ARM) unterschiedliche
# Wheels brauchen — ein hier eingefrorener Build wäre auf dem Pi nicht
# installierbar.
#
# Für echte Reproduzierbarkeit auf dem Ziel-Pi zusätzlich einmalig einen
# Lockfile erzeugen und committen:
#     venv/bin/pip freeze > requirements.pi.txt
# (siehe ROADMAP.md, P1-15)

gpiozero~=2.0.1
pygame~=2.6.1
opencv-python~=5.0.0
flask~=3.1.3
waitress~=3.0.2
qrcode[pil]~=8.2
pillow~=11.3.0
