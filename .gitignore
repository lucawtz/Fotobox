__pycache__/
*.pyc
*.pyo
Picture_Box/
thumbnails/
logs/
*.log
.flask_secret
config.json
# Papierzaehler (paper.py). Boxspezifischer Stand, so wie die config.json
# daneben — ein `git pull` auf dem Pi darf ihn nicht anfassen.
.paper_state.json

# Frontend Build-Artefakte
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo
frontend/vite.config.d.ts
frontend/vite.config.js

# Root-npm (dev-orchestration via concurrently)
node_modules/

# Generierte Script-Vorschaubilder
scripts/preview_*.png

# Lokales Python-venv (Entwicklung auf macOS)
venv/
.venv/

# Vom Admin-Panel hochgeladenes Logo (gallery_server.py schreibt nach
# Layout/logo.png). Das ist Mieter-Data und gehoert nicht ins Repo.
# Layout/logo_default.png (Box-Besitzer-Standard) bleibt bewusst trackbar.
Layout/logo.png

# Visuelles Test-Gerüst für Admin-Seiten (nicht im Vite-Entry, nicht im Build).
# Enthält einen fest eingebauten Admin-PIN samt Auto-Login — gehört nicht ins Repo.
frontend/dev-check.html
frontend/src/dev-check.tsx
